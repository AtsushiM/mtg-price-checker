<%= form_for(@card_price) do |f| %>
  <% if @card_price.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@card_price.errors.count, "error") %> prohibited this card_price from being saved:</h2>

      <ul>
      <% @card_price.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :card_id %><br>
    <%= f.number_field :card_id %>
  </div>
  <div class="field">
    <%= f.label :price %><br>
    <%= f.number_field :price %>
  </div>
  <div class="field">
    <%= f.label :time %><br>
    <%= f.datetime_select :time %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
